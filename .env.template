# .env.template
#
# It is a template file for .env file which contains all the environment
# variables used in the docker-compose.yml file.
#
# Should be copied to .env file and filled with the correct values.

# irem - irem configuration
DATA_IREM_RAW_DIR=

# irem orbit / magfield pipeline
# seed mode (default): stage bundled magfield_data_full.h5 instead of downloading
IREM_MAGFIELD_FETCH_MODE=seed
# Host path to the bundled HDF5 (repo root by default)
IREM_MAGFIELD_SEED_PATH=./magfield_data_full.h5
# IREM_MAGFIELD_CHUNK_ROWS=500000
# Set to network later when upstream URLs work again:
# IREM_MAGFIELD_FETCH_MODE=network

# irem pitch-angle pipeline
# seed mode (default): stage bundled irem_magnetic_pitch_angle_full.h5 (the
# actual output of a working 03_pitch_angle.ipynb run) instead of computing
IREM_PITCH_ANGLE_FETCH_MODE=seed
# Host path to the bundled HDF5 (repo root by default)
IREM_PITCH_ANGLE_SEED_PATH=./irem_magnetic_pitch_angle_full.h5
# IREM_PITCH_ANGLE_CHUNK_ROWS=500000
#
# network mode only: local directory of extracted INTEGRAL SPICE kernels
# (spk/ck/sclk/lsk/pck/fk/ik subfolders), furnished directly and offline -
# no ESA API lookup involved. Not needed in (default) seed mode.
IREM_KERNELS_SEED_PATH=./irem_kernels
# IREM_PITCH_ANGLE_INTERVAL_MINUTES=5
# Set to network later if you want to compute pitch angle from scratch:
# IREM_PITCH_ANGLE_FETCH_MODE=network

# influxdb - influxdb configuration
DOCKER_INFLUXDB_INIT_USERNAME=''
DOCKER_INFLUXDB_INIT_PASSWORD=''
DOCKER_INFLUXDB_INIT_ORG=''
DOCKER_INFLUXDB_INIT_BUCKET=''
DOCKER_INFLUXDB_INIT_ADMIN_TOKEN=''
DOCKER_INFLUXDB_INIT_IREM_BUCKET=''

# grafana - grafana configuration
GF_SECURITY_ADMIN_USER=
GF_SECURITY_ADMIN_PASSWORD=
GF_VIEWER_LOGIN=
GF_VIEWER_PASSWORD=

#prometheus
RETENTION_SIZE=5GB # size of machine benchmarking data to be retained

# elasticsearch
ELASTICSEARCH_USER=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_JAVA_OPTS=-Xms1g -Xmx1g   # Memory settings for ES JVM
ELASTICSEARCH_DISCOVERY_TYPE=single-node

# kibana
KIBANA_ELASTICSEARCH_USERNAME=
KIBANA_ELASTICSEARCH_PASSWORD=

# One-time full reprocessing (set to 1 to reprocess all existing data without re-downloading)
REPROCESS_ALL_DATA=0

#Chunk size for IREM uploads (rows per batch)
IREM_CHUNK_ROWS=500000

# Seconds-of-counting one IREM COUNT unit represents; divisor in
# COUNTS_PER_MIN = COUNT * (60 / IREM_CADENCE_SECONDS). IREM COUNTRATE is already a
# per-second rate, so COUNT is 1/sec and this must be 1 -> COUNTS_PER_MIN = COUNT * 60.
# Do NOT set it to the 60s sample spacing. Keep it defined (empty string crashes float()).
IREM_CADENCE_SECONDS=1

#RADEM chunk size for uploads (rows per batch)
RADEM_CHUNK_ROWS=100000